# PDv2 Round E / WP4 — kit `t1b-v39`: the tower room's bare walls and the hard ground seam — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. One Opus agent per task; the planner reviews between tasks and does the docs.

**Goal:** Kit `t1b-v39` (`KIT_VERSION` 27): the theme-2 alt-1 room (the "tower room", chunks 13001–13015) gets its six facades per wall back, and the ground-texture transition between MCNK edges is blended instead of pinned — measured by the audits, shipped through the documented chain, deployed with its chunk-meta SQL in one window.

**Architecture:** Two workspace scripts change. `48_gen_t1_blockkit.py` stops reserving eight MODF rows for a pad ring that Round B/B2 deleted (K1). `51_texture_blockkit.py` sets the MCNK header flag `0x8000` (`do_not_fix_alpha_map`), drops the asymmetric edge-row pin in `paint_block_alpha()` and widens the wall feather (K2). The chain 48 → 51 → 52 → 49 rebuilds the kit into the default staging, the audits prove both fixes, the DLL test proves the composer contract, and the deploy moves kit + SQL together.

**Tech Stack:** Python 3 workspace scripts (not git — `.pre_roundE_20260910` backups + `diff -u` files in `docs/superpowers/notes/`), the module's `data/sql/db-world/mod_pdungeon_chunk_meta.sql` (generated by 48), `pdblock` harness, `flstream_tests.exe`.

**Spec:** `docs/superpowers/specs/2026-09-10-pdv2-round-e-loot-talents-design.md` §1.6 (measured causes), §2 D14, §3 K1 + K2. Kit laws: `share-public/python_scripts/pdv2-kit/README.md` ("The chain", "Gates before a kit ships"), `docs/World of Warcraft/procedural-dungeon/03-city-facades.md` §"Reproducing the chain" and the deploy paragraph.

---

## Global Constraints

- **The chain order is load-bearing**: `48` (rewrites `kit_meta.json` from scratch, writes the module SQL) → `51 --kit <staging>` → `52 --kit <staging>` → `49 --manifest output\pd_live_manifest.txt`. Default staging `output\pd_block_kit\FLStream\chunks\t1b`; the DLL suite reads THAT path. Run every script with `PYTHONUTF8=1` from `C:\wowstuff\ForgottenLand2.0`.
- **Size-neutral only**: every kit file keeps its byte size (48 asserts one size; `49` is a fixed-size memcpy; the DLL copies header bytes verbatim). Alpha stays raw 4-bit / 2048 B; `nLayers` 2; `MCLY_FLAG_COMPRESSED` never set. A header flag bit and alpha VALUES are allowed; alpha depth, layer count and MODF row count (`PLACE_K_W = 24`) are not.
- **Gates before the kit ships** (README): two full chain runs → 247/247 files byte-identical; `51 --dry-run` all PASS, `52` ALL PASS, `49` ALL PASS; `fl-stream-client\build\Release\flstream_tests.exe` 497 checks / 0 with both "byte-identical to the oracle" lines; audits `105`, `104`, `103`, `102`, `99`, `101`; module harness `pdblock --batch 500 / --decor-batch 3000 / --roomcap 3000` = 0 failures (the walk masks do not move in this round — say so if a harness pin does).
- **Kit and SQL move together**: the deploy copies the kit to `C:\wowstuff\FL2-Client\Data\FLPD-Kit\t1b-v39`, the regenerated `mod_pdungeon_chunk_meta.sql` (`kitVersion` 27) is applied by the worldserver restart, and `FLStream.ini`'s `KitDir` flips LAST with the client CLOSED (a v39 kit on a kit-26 database = no walk masks). Nobody online during the restart (`SELECT COUNT(*) FROM characters WHERE online = 1` = 0).
- Workspace scripts are not git: back up `48_gen_t1_blockkit.py` and `51_texture_blockkit.py` as `*.pre_roundE_20260910` before the first edit; the vault copies in `share-public/python_scripts/pdv2-kit/` are refreshed at the close (controller).
- Branch `claude/pdv2-round-e-63ac9a2a` in `mod-procedural-dungeon` (HEAD `d850f4a`); Conventional Commits; the module commit carries the SQL and the notes diffs.

---

## File Structure

**Workspace (edited, backed up):** `C:\wowstuff\ForgottenLand2.0\scripts\48_gen_t1_blockkit.py` (K1 reserve, `KIT_VERSION`), `scripts\51_texture_blockkit.py` (K2 pin, flag, feather, gate).
**Workspace (generated):** `output\pd_block_kit\FLStream\chunks\t1b\*` (247 files), `output\pd_compose_blocks\*` (the composed operator tile).
**Module:** `data/sql/db-world/mod_pdungeon_chunk_meta.sql` (regenerated by 48), `docs/superpowers/notes/2026-09-10-round-e-kit-v39.md` + `task-1-script48-pad-reserve.diff` + `task-2-script51-alpha-pin-flag.diff`, `CLAUDE.md` (kit row), `README.md`.
**Client:** `C:\wowstuff\FL2-Client\Data\FLPD-Kit\t1b-v39\`, `C:\wowstuff\FL2-Client\FLStream.ini` (KitDir + ladder paragraph).

---

### Task 1: K1 — the pad-slot reserve, and the first chain run

**Files:**
- Modify: `C:\wowstuff\ForgottenLand2.0\scripts\48_gen_t1_blockkit.py` (`PAD_FACADE_SLOTS` `:1164-1169`; the alt-1 room call site `:2837-2843`; `KIT_VERSION` `:103`)
- Backup first: `48_gen_t1_blockkit.py.pre_roundE_20260910`

**Interfaces:**
- Consumes: `footprint_cells(2, ridx, alt)` (`:254-260`, the 2×2 pad only for theme 2 / alt 1 / `room`), `add_pad_facades()`'s small-pad branch (`:2451-2485`, one centred `house_d`), `PAD_MIN_SPAN_YD = 25.0` (`:1173`), `add_facades(..., reserve=...)` (`:2375`, `per_side = (PLACE_K_W − reserve) // 4` at `:2389`).
- Produces: the reserve becomes pad-size-aware — `1` when the pad's span is below `PAD_MIN_SPAN_YD` (one centred building), `PAD_FACADE_SLOTS` (8) only for a ring pad ≥ 25 yd (kept for a pad that could come back; comment says so). `KIT_VERSION = 27`.

- [ ] **Step 1: Backup + edit.** Copy the script to `.pre_roundE_20260910`. At the call site (`:2837-2843`) compute the pad span from `footprint_cells` (2 cells × 8.33 yd = 16.67 yd; read how `add_pad_facades` derives `span` and reuse that expression) and pass `reserve = (1 if span < PAD_MIN_SPAN_YD else PAD_FACADE_SLOTS) if pad else 0`. Update the `PAD_FACADE_SLOTS` comment block (`:1164-1169`): it still describes the 33 yd boss pad. `KIT_VERSION = 27`.
- [ ] **Step 2: Build once.** `python scripts\48_gen_t1_blockkit.py` (default staging). Read the summary: 247 files, 244 chunk rows, `kitVersion 27`; `kit_meta.json` `wmoPlacements` for 13001–13015 now min/mean/max ≈ 21 / 23.2 / 24 like the alt-0 family (the shipped v38 had 19 / 21.2 / 24 — quote both). Then `python scripts\51_texture_blockkit.py --kit output\pd_block_kit\FLStream\chunks\t1b`, `python scripts\52_punch_kit_holes.py --kit output\pd_block_kit\FLStream\chunks\t1b`, `python scripts\49_pd_compose_blocks.py --manifest output\pd_live_manifest.txt` (read `--help` for the exact `--kit`/`--out` defaults; ALL CHECKS PASS).
- [ ] **Step 3: Measure K1.** `PYTHONUTF8=1 python scripts\105_frontage_audit.py --min-run 0.5 --top 60` — baseline was `81 yd bare of 5 536 (1.5 %)`, every bare yard on 13005/13006/13010. Expect ≈ 0 on those three chunks; quote the new total and the per-chunk list. If bare yards remain on the tower rooms, report which stations (the known "run house ground box covers the jamb cell while its body ends short" residue is acceptable if it is the same 2–3 stations 105 always listed; anything new is not).
- [ ] **Step 4: Record.** `diff -u scripts\48_gen_t1_blockkit.py.pre_roundE_20260910 scripts\48_gen_t1_blockkit.py > <module>\docs\superpowers\notes\task-1-script48-pad-reserve.diff` (LF). Do not commit yet (Task 3 commits SQL + notes together). Report the numbers.

---

### Task 2: K2 — the MCNK flag, the edge pin, the feather; gates and determinism

**Files:**
- Modify: `C:\wowstuff\ForgottenLand2.0\scripts\51_texture_blockkit.py` (`WRITABLE_HEADER_FIELDS` `:189-190`, `FEATHER_TEXELS` `:476`, `paint_block_alpha()` `:526-612` — the pin at `:588-604`, the verify gate `_gate_wall_distance()` `:777-818` + the envelope check `:1010-1032`)
- Backup first: `51_texture_blockkit.py.pre_roundE_20260910`

**Interfaces:**
- Consumes: the ADT writer's MCNK header access (`adt.py`/`adt_writer.py` in the vault copy or the workspace `pd_adt_lib.py` — find which module 51 uses to write MCNK fields and whether the flags dword at MCNK header offset `0x00` is reachable through `WRITABLE_HEADER_FIELDS`); `MCNK_FLAG_DO_NOT_FIX_ALPHA = 0x8000` (name it).
- Produces: every kit chunk's every MCNK carries `flags |= 0x8000`; `paint_block_alpha()` ramps the wall feather on every texel including the outermost MCNK row/column (the `on_mcnk_edge` term is gone, the comment rewritten: the flag makes the client render the 64×64 alpha as authored, so the two sides of an MCNK edge now agree); `FEATHER_TEXELS = 6`; the verify gate mirrors the same rule (no edge exception). Still raw 4-bit, 2048 B.

- [ ] **Step 1: Backup + the flag writer.** Read how 51 writes MCNK header fields (`WRITABLE_HEADER_FIELDS` and the function that applies them) and how `52_punch_kit_holes.py` writes the `holes` header field (it already edits MCNK headers size-neutrally — copy that idiom). Add the flags dword (offset `0x00` of the MCNK header) to the writable set, OR the bit in when rebuilding the chunk. Verify by reading one chunk back (`struct.unpack` at the MCNK header) that the bit is set on every MCNK of every kit file and no other header byte moved.
- [ ] **Step 2: The pin and the feather.** Remove the `on_mcnk_edge` exception (`:597-604`), set `FEATHER_TEXELS = 6`, mirror in `_gate_wall_distance()` / the envelope check, rewrite the comment block (`:588-596`) — it must say why the flag lets the pin go and that `52` deferred the same fix.
- [ ] **Step 3: Chain + gates.** Full chain from 48 (48 → 51 → 52 → 49); `51 --dry-run` all PASS; `52` ALL PASS; `49` ALL CHECKS PASS; the DLL suite `C:\Users\Anwender\Documents\GitHub\fl-stream-client\build\Release\flstream_tests.exe` (needs `output\pd_compose_blocks\manifest.txt` from `pdblock --manifest 297397130 output\pd_compose_blocks\manifest.txt <rooms> <obx> <oby> <theme> <bossRooms>` — read the README paragraph and the harness usage for the argument list; if the suite's expectations embed kit hashes and fail ONLY on those, report it as such — the contract is the composer byte-identity, not a fixed kit) → 497 / 0 with both "byte-identical to the oracle" lines. **Determinism**: run the chain a second time into a side directory (`48 --out <side>` then 51/52 with `--kit <side>`) and compare all 247 files byte-for-byte with the staging (`fc /b` or a Python loop) → 247/247 identical.
- [ ] **Step 4: Measure K2 (and K1 again).** `103_floor_seam_audit.py --top 10`: baseline block-boundary max step 10, mean 2.31, side means 6.0 vs 7.8; target max ≤ 4 and equal side means (the artefact-free reference is `same block corridor` max 4 / mean 0.10). `105` (K1 still ≈ 0 on the tower rooms), `104`, `102`, `99`, `101` — all as the README lists; quote each script's verdict line. Module harness: `pdblock --batch 500`, `--decor-batch 3000`, `--roomcap 3000` = 0 failures (the masks did not move; if a pin moves, stop and report).
- [ ] **Step 5: Record.** `diff -u` of 51 into `<module>\docs\superpowers\notes\task-2-script51-alpha-pin-flag.diff`. Do not commit yet.

---

### Task 3: Deploy kit v39 + the chunk-meta SQL, restart, commit

**Files:**
- Create: `C:\wowstuff\FL2-Client\Data\FLPD-Kit\t1b-v39\` (247 files)
- Modify: `C:\wowstuff\FL2-Client\FLStream.ini` (`KitDir=Data\FLPD-Kit\t1b-v39`, plus the round's paragraph in the version ladder — read the v37/v38 paragraphs and write v39's in the same voice: what changed, kitVersion 27, rollback = KitDir back to v38 AND `DELETE FROM acore_world.pdungeon_chunk_meta WHERE kitVersion = 27;` then re-apply the v38 SQL, client cold)
- Commit (module): `data/sql/db-world/mod_pdungeon_chunk_meta.sql` (regenerated), `docs/superpowers/notes/task-1-*.diff`, `task-2-*.diff`, `docs/superpowers/notes/2026-09-10-round-e-kit-v39.md` (the audit numbers before/after, the gate results, the deploy md5s)

- [ ] **Step 1: Pre-flight.** `tasklist | findstr /i "ForgottenLand Wow.exe"` — the client must be CLOSED; `SELECT COUNT(*) FROM characters WHERE online = 1` = 0 (read-only via `docs/superpowers/notes/round-e-loot-sql/q.sh <file> acore_characters`). `git -C <module> diff --stat data/sql/db-world/mod_pdungeon_chunk_meta.sql` shows the kitVersion 26 → 27 rows (and the changed sha256/size values of every chunk — K2 touched every file).
- [ ] **Step 2: Kit copy.** `robocopy output\pd_block_kit\FLStream\chunks\t1b C:\wowstuff\FL2-Client\Data\FLPD-Kit\t1b-v39 /E` (247 files; verify count and a byte compare of `kit_meta.json`).
- [ ] **Step 3: SQL + restart.** Commit the SQL + notes in the module (`feat(kit): t1b-v39 - tower-room facades restored, MCNK 0x8000 + unpinned alpha feather (kitVersion 27)`, trailer `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`). Stop the worldserver (`Stop-Process -Name worldserver` — nobody online), start it again from `C:\wowstuff\dcore` (`Start-Process worldserver.exe -WorkingDirectory C:\wowstuff\dcore`), wait for `World Initialized`, grep the boot log: `Reapplying update "mod_pdungeon_chunk_meta.sql"`, `244 walk mask(s) … 244 with a patrol clearance layer`, no error above the baseline; `SELECT DISTINCT kitVersion FROM pdungeon_chunk_meta` = 27 (read-only query).
- [ ] **Step 4: Ini flip LAST.** Back up `FLStream.ini` as `FLStream.ini.pre_v39_20260910`, set `KitDir=Data\FLPD-Kit\t1b-v39`, append the ladder paragraph. Report the md5 of `kit_meta.json` in the client kit and the staging (identical).

---

### Task 4: Docs (controller)

`CLAUDE.md` kit row (`t1b-v37 / KIT_VERSION 25` → v39 / 27, K1/K2 in one sentence each), `README.md` (a Round E kit sentence), the vault `python_scripts/pdv2-kit/` copies of 48 + 51 refreshed + a v39 paragraph in its README, `03-city-facades.md` (the throat-facade item: resolved — it was the pad reserve), `12-server-todo.md` (the throat-facade decision row removed), runde31 section 7 (what to look at: the tower room's walls, the floor at MCNK edges and room/corridor joins), `claude_log.md` END, MIG note at merge.
